---
marp: true
---

# 🧩 软件需求分析说明书（SRS）

> **文档版本**：v1.0  
> **编写日期**：2025-11-03  
> **编写人**：蒋莹莹  
> **项目名称**：StyleUnify —— 代码风格一致性管理插件

---



## 1. 项目背景

各种大语言模型在软件开发中的参与度持续增加，既加快了代码生成的速度，也拓宽了代码库的来源渠道。 然而，大语言模型在进行代码补全和生成时，通常只关注功能的正确性，而忽略与上下文代码或现有代码库的风格一致性。这导致生成的代码在格式、命名等方面可能与项目现有代码不统一。为了保证项目代码库的风格统一、提高代码可读性与可维护性，同时减轻代码审查的负担，我们开发**StyleUnify** 插件来实现自动化的代码风格一致性管理。

---



## 2. 项目概述

### 2.1 项目目标
- 风格不一致检测：插件应能够实时检测代码中与现有代码库不一致的部分，并在编辑过程中提供即时反馈。  
- 风格修复建议：插件应能够针对检测到的风格问题提供详细修复建议。  
- 自动风格修复：插件应支持一键自动修复检测到的风格问题。

### 2.2 系统范围
- 插件运行于2023.1及以上版本的 IntelliJ IDEA；
- 支持Java语言



---



## 3. 功能性需求

### 用例图

<img src="C:\Users\dell\AppData\Roaming\Typora\typora-user-images\image-20251103192229759.png" alt="image-20251103192229759" style="zoom:50%;" />

### 功能结构图


### 3.2 功能需求列表

| 功能编号 | 功能名称 | 功能描述 | 触发方式 |
|-----------|-----------|-----------|-----------|
| F1 | 代码风格检测 | 扫描项目文件，检测是否符合指定风格规则 | 菜单或快捷键 |
| F2 | 自动格式修复 | 一键修复格式不符文件 | 菜单或保存时触发 |
| F3 | 远程配置同步 | 从远程仓库同步最新风格模板 | 手动同步或定时任务 |
| F4 | 报告与提示 | 在工具窗口显示检测结果与统计信息 | 自动更新 |
| F5 | 插件设置 | 用户自定义远程地址、忽略规则 | 设置面板 |

### 3.3 详细功能描述

#### F1. 代码风格检测
- **输入**：项目路径、风格配置文件；
- **输出**：检测报告（JSON格式）；
- **前置条件**：项目已打开；
- **后置条件**：生成检测结果；
- **异常处理**：配置文件缺失时给出提示。

#### F2. 自动格式修复
- **输入**：检测结果；
- **输出**：已修复的文件；
- **约束**：仅修改格式相关内容；
- **反馈**：修复后在报告中标记为“已修复”。

#### F3. 远程配置同步
- **输入**：远程仓库URL；
- **输出**：更新本地配置；
- **支持**：Git / HTTP / 企业内部API；
- **异常处理**：网络错误时提示并回退。

#### F4. 报告与提示
- **界面**：插件工具窗口；
- **功能**：展示统计数据、违规详情、文件定位；
- **扩展**：可导出为 JSON / HTML 报告。

#### F5. 插件设置
- **位置**：`Settings → Tools → CodeStyleGuard`
- **可配置项**：
    - 远程配置地址；
    - 自动检测开关；
    - 忽略文件列表；
    - 检测规则级别。

---



## 4. 非功能性需求

| 类型 | 说明 |
|------|------|
| **性能** | 扫描 1,000 个文件应在 5 秒内完成；插件加载 < 2 秒 |
| **兼容性** | 支持 IntelliJ IDEA 2022.3+、JDK 17+ |
| **安全性** | 不上传源代码，仅传输风格配置文件 |
| **可维护性** | 使用 Kotlin + Gradle；支持单元测试与日志记录 |
| **可用性** | 所有主功能三步内可达 |
| **可扩展性** | 支持新语言风格规则插件化扩展 |

---



## 5. 界面与交互设计

### 5.1 菜单结构


### 5.2 工具窗口布局
| 区域 | 内容 |
|------|------|
| 左侧 | 检测结果列表（文件、问题数量） |
| 右侧 | 问题详情与修复建议 |
| 底部 | 操作按钮：Fix All / Refresh / Export |

---

## 6. 数据需求

| 数据项 | 类型 | 来源 | 说明 |
|---------|------|------|------|
| 风格配置 | JSON/YAML | 本地或远程 | 定义项目风格规范 |
| 检测结果 | JSON | 插件运行时生成 | 记录不符合规则的文件 |
| 用户配置 | JSON | 插件存储 | 保存自定义设置 |
| 日志记录 | TXT | 本地生成 | 记录执行过程与异常信息 |

---

## 7. 外部接口需求

| 接口类型 | 名称 | 说明 |
|-----------|------|------|
| IntelliJ API | com.intellij.psi.*, com.intellij.openapi.project.* | 实现代码扫描与修复 |
| 文件接口 | 本地配置文件 | 读写 `.editorconfig` 或自定义模板 |
| 网络接口 | REST API / Git | 从远程仓库拉取统一规则 |

---

## 8. 系统约束与假设

| 类型 | 说明 |
|------|------|
| 平台限制 | 仅支持 IntelliJ IDEA 平台 IDE |
| 网络依赖 | 远程同步功能需联网 |
| 权限限制 | 插件仅访问项目路径内文件 |
| 假设条件 | 用户具备基础 Git/HTTP 网络访问权限 |

---

## 9. 系统流程图

```text
用户操作
   ↓
触发插件菜单或保存事件
   ↓
读取项目配置与规则
   ↓
执行检测逻辑（PsiFile 分析）
   ↓
生成报告并更新 UI
   ↓
用户选择修复或同步配置
   ↓
操作完成并保存结果
